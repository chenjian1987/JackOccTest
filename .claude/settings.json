{
  "permissions": {
    "allow": [
      "Bash(cmd /c build.bat)",
      "Bash(cmd /c \"build.bat 2>&1\")",
      "Bash(cmd /c \"where msbuild\")",
      "Bash(powershell -Command \"where msbuild\")"
    ]
  }
}
